def extract_relevant_sections(raw_text: str) -> str:
    """
    Procesa el texto crudo extraído para filtrar y extraer únicamente
    las secciones con información médica relevante.

    Parámetros:
    -----------
    raw_text : str
        Texto completo extraído de la página (sin procesar).

    Retorna:
    --------
    str
        Texto limpio y procesado, que contiene solo la información útil
        para la base documental.
    """
    ...
